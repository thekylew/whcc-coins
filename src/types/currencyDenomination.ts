// Define a class for currency denomination
export default class CurrencyDenomination {
  name: string;
  value: number;

  constructor(name: string, value: number) {
    this.name = name;
    this.value = value;
  }
}
